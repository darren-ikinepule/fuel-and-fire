# Backend API for food items (MongoDB CRUD)
VITE_API_URL=http://localhost:3000

# Gemini key for /api/generate (server-side only — set in Vercel as GEMINI_API_KEY)
GEMINI_API_KEY=your-key-from-https://aistudio.google.com/apikey
